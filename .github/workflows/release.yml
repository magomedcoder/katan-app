# Сборка и публикация при создании тега v*
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      version: ${{ github.ref_name }}
    secrets: inherit

  release:
    name: Upload GitHub Release
    needs: [build]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          VER="${GITHUB_REF_NAME#v}"
          [[ "$VER" =~ ^[0-9] ]] || VER="1.0.0"
          echo "VER=$VER" >> $GITHUB_ENV

          cd artifacts

          if [ -f android-apk/app-release.apk ]; then
            mv android-apk/app-release.apk ../katan-android-$VER.apk
          else
            echo "APK not found!" && exit 1
          fi

          if [ -d ios-app ]; then
            mv ios-app ../katan-ios-$VER.app
          else
            echo "ios-app not found!" && exit 1
          fi

          if [ -f linux-installer/katan-linux.tar.gz ]; then
            mv linux-installer/katan-linux.tar.gz ../katan-linux-$VER.tar.gz
          else
            echo "linux-installer artifact not found!" && exit 1
          fi

          DEB=$(find linux-installer -maxdepth 1 -name 'katan-*-amd64.deb' -print -quit)
          if [ -z "$DEB" ] || [ ! -f "$DEB" ]; then
            DEB="linux-installer/katan-amd64.deb"
          fi
          if [ -f "$DEB" ]; then
            cp "$DEB" ../katan-linux-$VER.deb
          else
            echo "linux .deb not found! Contents of linux-installer:"
            ls -la linux-installer/
            exit 1
          fi

          if [ -f windows-installer/KatanSetup.exe ]; then
            mv windows-installer/KatanSetup.exe ../katan-windows-$VER.exe
          elif [ -f KatanSetup.exe ]; then
            mv KatanSetup.exe ../katan-windows-$VER.exe
          else
            echo "windows-installer artifact not found!" && exit 1
          fi

          if [ -f macos-installer/katan-macos.dmg ]; then
            mv macos-installer/katan-macos.dmg ../katan-macos-$VER.dmg
          else
            echo "macos-installer artifact not found!" && exit 1
          fi

      - name: Upload Release to GitHub
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            katan-android-${{ env.VER }}.apk
            katan-ios-${{ env.VER }}.app
            katan-linux-${{ env.VER }}.tar.gz
            katan-linux-${{ env.VER }}.deb
            katan-windows-${{ env.VER }}.exe
            katan-macos-${{ env.VER }}.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
