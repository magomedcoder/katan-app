# Сборка при push в main и обновление тега dev-build
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      version: ${{ github.event_name == 'push' && 'dev-build' || (github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number)) || 'dev-build' }}
    secrets: inherit

  update-dev-build-tag:
    name: Update dev-build tag
    needs: [build]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Delete and recreate dev-build tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -d dev-build 2>/dev/null || true
          git push origin :refs/tags/dev-build 2>/dev/null || true
          git tag dev-build
          git push origin dev-build
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  dev-release:
    name: Upload Dev-Build Release
    needs: [build, update-dev-build-tag]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare dev-build artifacts
        run: |
          cd artifacts

          if [ -f android-apk/app-release.apk ]; then
            mv android-apk/app-release.apk ../katan-android-dev.apk
          else
            echo "APK not found!" && exit 1
          fi

          if [ -d ios-app ]; then
            mv ios-app ../katan-ios-dev.app
          else
            echo "ios-app not found!" && exit 1
          fi

          if [ -f linux-installer/katan-linux.tar.gz ]; then
            mv linux-installer/katan-linux.tar.gz ../katan-linux-dev.tar.gz
          else
            echo "linux-installer artifact not found!" && exit 1
          fi

          DEB=$(find linux-installer -maxdepth 1 -name 'katan-*-amd64.deb' -print -quit)
          if [ -z "$DEB" ] || [ ! -f "$DEB" ]; then
            DEB="linux-installer/katan-amd64.deb"
          fi
          if [ -f "$DEB" ]; then
            cp "$DEB" ../katan-linux-dev.deb
          else
            echo "linux .deb not found! Contents of linux-installer:"
            ls -la linux-installer/
            exit 1
          fi

          if [ -f windows-installer/KatanSetup.exe ]; then
            mv windows-installer/KatanSetup.exe ../katan-windows-dev.exe
          elif [ -f KatanSetup.exe ]; then
            mv KatanSetup.exe ../katan-windows-dev.exe
          else
            echo "windows-installer artifact not found!" && exit 1
          fi

          if [ -f macos-installer/katan-macos.dmg ]; then
            mv macos-installer/katan-macos.dmg ../katan-macos-dev.dmg
          else
            echo "macos-installer artifact not found!" && exit 1
          fi

      - name: Upload Dev-Build Release to GitHub
        uses: softprops/action-gh-release@v2
        with:
          tag_name: dev-build
          name: "Dev build"
          draft: false
          prerelease: true
          files: |
            katan-android-dev.apk
            katan-ios-dev.app
            katan-linux-dev.tar.gz
            katan-linux-dev.deb
            katan-windows-dev.exe
            katan-macos-dev.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
